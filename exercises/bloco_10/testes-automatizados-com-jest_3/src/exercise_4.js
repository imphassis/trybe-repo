const upperString = (string) => string.toUpperCase();

const firstChar = (string) => string[0];

const concatStrings = (string1, string2) => string1.concat(string2);

module.exports = { upperString, firstChar, concatStrings };
