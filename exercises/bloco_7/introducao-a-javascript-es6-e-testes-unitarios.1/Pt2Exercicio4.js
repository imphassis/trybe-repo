const lista = ['JavaScript', 'Python', 'GitHub', 'ES6', 'DevOps'];

function CharChanger(text, param) {
  let str = text.replace('x', param);

  console.log(str);
}

const texaco = (beta) => {
  let orderedList = beta.sort();
  CharChanger('Tryber x aqui!', 'PH');
  console.log(`Minhas principais skills são:`);
  for (let i = 0; i < orderedList.length; i += 1) {
    content;
  }
};

texaco(lista);
